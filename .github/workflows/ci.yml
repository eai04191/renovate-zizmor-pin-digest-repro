name: CI

on:
  workflow_dispatch:

jobs:
  zizmor:
    runs-on: ubuntu-latest
    steps:
      - uses: zizmorcore/zizmor-action@70fb788f84895a7701f5643d103d587e460b5c99 # v0.6.3
        with:
          version: "1.28.0"
